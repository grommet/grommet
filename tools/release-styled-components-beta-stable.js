import del from"del";import fs from"fs-extra";import git from"simple-git/promise";import path from"path";const repoURL=`https://${process.env.GH_TOKEN}@github.com/grommet/grommet.git`,localFolder=path.resolve(".tmp/grommet"),localDist=path.resolve("dist");process.env.CI?del(localFolder).then((()=>{git().silent(!1).clone(repoURL,localFolder).then((()=>git(localFolder).checkout("styled-components-beta-stable"))).then((()=>del([`${localFolder}/**/*`]))).then((()=>fs.copy(localDist,localFolder))).then((()=>git(localFolder).add(["--all","."]))).then((()=>git(localFolder).commit("styled-components-beta-stable updated"))).then((()=>git(localFolder).push("origin","styled-components-beta-stable"))).catch((e=>console.error("failed: ",e)))})):console.warn("Skipping release. Release:styled-components-beta-stable task \n    should be executed by CI only.");