import * as React from 'react';
import RadioButtonGroupInteractive from '../RadioButtonGroupInteractive';

export default (
  <RadioButtonGroupInteractive
    uxpId="radiogroup0"
    name="doc"
    options={['one', 'two']}
    value="one"
  />
);
