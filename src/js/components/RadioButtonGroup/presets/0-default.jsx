import * as React from 'react';
import RadioButtonGroup from '../RadioButtonGroup';

export default (
  <RadioButtonGroup
    uxpId="radiogroup0"
    name="doc"
    options={['one', 'two']}
    value="one"
  />
);
