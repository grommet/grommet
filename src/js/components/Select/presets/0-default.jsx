import * as React from 'react';
import Select from '../Select';

export default (
  <Select
    uxpId="select0"
    options={['small', 'medium', 'large']}
    value='medium'
    style={{"width": "100%"}}
  />
);
