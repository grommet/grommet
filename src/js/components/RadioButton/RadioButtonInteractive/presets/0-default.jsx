import * as React from 'react';
import RadioButtonInteractive from '../RadioButtonInteractive';

export default (
  <RadioButtonInteractive
    uxpId="radio0"
    name="choices"
    label="chosen"
  />
);
