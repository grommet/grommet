import * as React from 'react';
import Meter from '../Meter';

export default (
  <Meter
    uxpId="meter0"
    values={[
      {
        value: 60,
        label: 'sixty',
      },
    ]}
    aria-label="meter"
  />
);
