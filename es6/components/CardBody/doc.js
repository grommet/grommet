import { describe } from 'react-desc';
export var doc = function doc(CardBody) {
  var DocumentedAccordionPanel = describe(CardBody).description('The Card Body.').intrinsicElement('div');
  return DocumentedAccordionPanel;
};